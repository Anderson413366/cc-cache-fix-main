{
  "name": "cc-cache-fix-claude-code-runtime",
  "private": true,
  "description": "Pinned Claude Code runtime used by the local cache-fix installer.",
  "scripts": {
    "audit": "npm audit --omit=dev",
    "install:runtime": "npm ci --ignore-scripts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "2.1.84"
  }
}
